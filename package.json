{
  "name": "gpt-link-hub",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix project-root run dev",
    "build": "npm --prefix project-root run build",
    "preview": "npm --prefix project-root run preview"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.11",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.0",
    "vite": "^5.4.19"
  }
}
